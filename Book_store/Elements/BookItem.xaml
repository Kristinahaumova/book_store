<UserControl x:Class="Book_store.Elements.BookItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Book_store.Elements"
             Name="bookItem">
    <UserControl.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="White" Offset="0"/>
            <GradientStop Color="#F8F9FA" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>
    <Border Background="Transparent" 
            BorderBrush="#E0E0E0" 
            BorderThickness="1" 
            CornerRadius="8" 
            Padding="15" 
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="1" BlurRadius="3" Opacity="0.2"/>
        </Border.Effect>
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#E3F2FD"/>
                        <Setter Property="Cursor" Value="Hand"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="{Binding Name}" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="#2C3E50" 
                       Margin="0,0,0,8" 
                       TextWrapping="Wrap"/>

            <TextBlock Grid.Row="1" 
                       Text="{Binding AuthorName}" 
                       FontSize="14" 
                       FontWeight="SemiBold" 
                       Foreground="#7F8C8D" 
                       Margin="0,0,0,10"/>

            <TextBlock Grid.Row="2" 
                       Text="{Binding Price, StringFormat='Цена: {0} руб.'}" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Foreground="#27AE60" 
                       Margin="0,0,0,10"/>

            <Button Grid.Row="3" 
                    Content="Купить" 
                    Click="addCart" 
                    Width="80" 
                    Height="35"
                    Background="#27AE60" 
                    Foreground="White" 
                    BorderThickness="0" 
                    FontSize="12" 
                    FontWeight="Bold" 
                    Cursor="Hand"
                    HorizontalAlignment="Right">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#229954"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</UserControl>
